import { TAX_EXEMPTION_OPTIONS } from "./app/lib/tax-exemptions.js";
import fs from "fs";

// Need to compile ts to js or just read it.
// It's easier to just read the file and extract the labels/values via regex.
